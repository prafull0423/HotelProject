import { MobValidationDirective } from './mob-validation.directive';

describe('MobValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new MobValidationDirective();
    expect(directive).toBeTruthy();
  });
});
